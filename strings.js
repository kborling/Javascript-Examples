var name = "Kevin";

var last = "kevin"


if(name === last)
	console.log("Strings are equal");
else
	console.log("Strings aren't equal");


// a < b true
function compare(a, b) {
	console.log(a + " < " + b, a < b);
}

compare('a', 'b');